server:
  port: 8012

spring:
  application:
    name: springcloud-consumer-order8012

eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:7011/eureka